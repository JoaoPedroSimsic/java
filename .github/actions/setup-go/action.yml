name: Setup Go
description: Go 1.24 with module dependency cache
runs:
  using: composite
  steps:
    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.24'
        cache-dependency-path: gateways/ws-gateway/go.sum
