APP_PORT=8080

PROFILE=dev

POSTGRES_URL=jdbc:postgresql://db:5432/postgres
POSTGRES_DB=postgres
POSTGRES_USER=root
POSTGRES_PASSWORD=password
POSTGRES_PORT=5432

FLYWAY_USER=flyway_user
FLYWAY_PASSWORD=password

APP_USER=app_user
APP_PASSWORD=password

# =============================================================================
# GATEWAY CONFIGURATION
# =============================================================================
# The gateway authenticates users and forwards requests to backend services.
# 
# DEVELOPMENT: Uses Spring Cloud Gateway (docker-compose)
#   - Gateway runs as a container alongside services
#   - Uses Keycloak for authentication
#   - Configured via docker-compose.yml and gateway/application-dev.yml
#
# PRODUCTION: Uses AWS API Gateway + Lambda Authorizer
#   - API Gateway handles routing and rate limiting
#   - Lambda authorizer validates JWT tokens from Auth0
#   - Configured via Terraform (infrastructure/terraform/)
#
# Both gateways inject the same headers to backend services:
#   - X-Gateway-Secret: Shared secret for service-to-service auth
#   - X-User-Id: User ID from identity provider (sub claim)
#   - X-User-Email: User email from identity provider
# =============================================================================

# Gateway-to-service authentication secret (used by both Spring Cloud Gateway and AWS API Gateway)
GATEWAY_SECRET=your-secure-gateway-secret-change-in-production

# =============================================================================
# DEVELOPMENT: Keycloak (used by Spring Cloud Gateway)
# =============================================================================
# JWKS_URL and JWT_ISSUER are set in docker-compose.yml
# Keycloak admin console: http://localhost:8180 (admin/admin)
# Test users:
#   - test@example.com / password
#   - admin@example.com / password

# =============================================================================
# PRODUCTION: Auth0 (used by AWS API Gateway Lambda Authorizer)
# =============================================================================
# Set these in your Terraform tfvars or environment:
#   jwks_url       = "https://YOUR_DOMAIN.auth0.com/.well-known/jwks.json"
#   jwt_issuer     = "https://YOUR_DOMAIN.auth0.com/"
#   gateway_secret = "your-production-secret"

REDIS_HOST=redis
REDIS_PORT=6379

AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key

RABBITMQ_HOST=rabbitmq
RABBITMQ_PORT=5672
RABBITMQ_USERNAME=guest
RABBITMQ_PASSWORD=guest
