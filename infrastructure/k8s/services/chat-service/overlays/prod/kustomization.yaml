apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- ../../base
configMapGenerator:
- name: chat-service-config
  envs:
  - params.env
labels:
- pairs:
    environment: prod
  includeSelectors: true
