apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- ../../base
configMapGenerator:
- name: user-postgres-config
  envs:
  - params.env
labels:
- pairs:
    environment: dev
  includeSelectors: true
