apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - bundle.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: hermes
      app.kubernetes.io/component: external-secrets-crds
