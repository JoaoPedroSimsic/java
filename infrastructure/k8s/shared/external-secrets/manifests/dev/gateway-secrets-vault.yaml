apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: gateway-secrets-vault
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-hermes-dev
    kind: ClusterSecretStore
  target:
    name: gateway-secrets-vault
    creationPolicy: Owner
  data:
    - secretKey: GATEWAY_SECRET
      remoteRef:
        key: hermes/dev/shared/jwt-signing-key
        property: value
