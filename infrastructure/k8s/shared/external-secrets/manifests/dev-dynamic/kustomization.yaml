apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: hermes-dev

resources:
  - ../dev
  - auth-service-app-dynamic.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: hermes
      environment: dev
      secret-source: vault-dynamic
