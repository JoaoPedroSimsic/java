apiVersion: apps/v1
kind: Deployment
metadata:
  name: __DB_DEPLOY__
  labels:
    app: __DB_DEPLOY__
    component: database
spec:
  replicas: 1
  selector:
    matchLabels:
      app: __DB_DEPLOY__
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: __DB_DEPLOY__
        component: database
    spec:
      containers:
      - name: postgres
        image: postgres:16-alpine
        ports:
        - containerPort: 5432
          name: postgres
        envFrom:
        - configMapRef:
            name: __DB_DEPLOY__-config
        - secretRef:
            name: __PG_SECRETS__
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        volumeMounts:
        - name: data
          mountPath: /var/lib/postgresql/data
          subPath: postgres
        - name: init-script
          mountPath: /docker-entrypoint-initdb.d
        livenessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - $(POSTGRES_USER)
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - $(POSTGRES_USER)
          initialDelaySeconds: 5
          periodSeconds: 5
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: __DB_DEPLOY__-data
      - name: init-script
        configMap:
          name: __DB_DEPLOY__-init-script
          defaultMode: 0755
