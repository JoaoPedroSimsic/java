apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- deployment.yaml
- service.yaml
- pvc.yaml
- configmap.yaml
labels:
- pairs:
    app: __DB_DEPLOY__
  includeSelectors: true
- pairs:
    managed-by: kustomize
  includeSelectors: true
