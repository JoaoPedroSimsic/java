# Skaffold file watching / sync — keep this tight; repo root is the Docker context for Java services.

# Frontend: huge trees (node_modules, .angular); run `make front` separately
frontend/

# Docs / editor / CI — not part of container images
docs/
.claude/
.github/

# Infra not used by Skaffold kustomize paths in skaffold.yaml (see infrastructure/k8s/clusters/dev)
infrastructure/terraform/
infrastructure/lambda/
infrastructure/archive/

# Node anywhere (e.g. lambda tooling)
**/node_modules

# Build outputs
**/target
**/.angular
**/dist

# VCS / IDE
.git
.idea
.vscode
**/.DS_Store
